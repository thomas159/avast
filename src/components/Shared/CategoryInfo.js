import React from 'react'

const CategoryInfo = ({svg, title, device, price, buttonText}) => {
  return (
    <div>
      
    </div>
    SVG
    title
    devicePixelRatio
    price
    buttonText
  )
}